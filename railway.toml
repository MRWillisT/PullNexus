"$schema" = "https://railway.com/railway.schema.json"

[build]
builder = "RAILPACK"
buildCommand = "pip install --no-cache-dir '.[mcp]'"

[deploy]
startCommand = "pullnexus serve --transport http --host 0.0.0.0 --port ${PORT:-7337}"